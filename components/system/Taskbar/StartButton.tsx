import React from 'react';
import StyledStartButton from '../../../styles/components/system/Taskbar/StyledStartButton';

const StartButton = () => (
  <StyledStartButton type="button" title="Start">ICON</StyledStartButton>
);

export default StartButton;
