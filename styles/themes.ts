import Themes from "../types/styles/themes";


const defaultTheme = {
  colors: {
    backgroundcolor: "#000",
    primary: "#000",
    window: "#CCCCCC",
  },
};

const themes: Themes = { defaultTheme };

export default themes;
